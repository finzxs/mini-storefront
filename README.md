# Mini Storefront (React + Next.js App Router)

A small storefront demonstrating components/props, state, controlled inputs, effects with cleanup, lifting state, client vs server components, API routes, and Tailwind styling.

## Setup
```bash
npm install
npm run dev
